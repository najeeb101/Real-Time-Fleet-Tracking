# Written before scaffolding so no secret can ever reach a commit.
# create-next-app will extend this; re-check that `.env*.local` survives.

# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# Regenerated by `next dev` on every run (see
# node_modules/next/dist/server/lib/generate-agent-files.js). Ignored rather than
# committed to keep AI-tooling boilerplate out of the submission.
AGENTS.md
CLAUDE.md

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — .env.example is the only one that gets committed
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
